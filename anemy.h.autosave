#ifndef ANEMY_H
#define ANEMY_H
#include "hero.h"
#include <QObject>

class Anemy : public QObject
{
    Q_OBJECT
public:
    explicit anemy(QObject *parent = nullptr,int mPosX = 0,int mPosY = 490);
    //出生位置
    int PosX;
    int PosY;
    //可活动最大范围
    int MaxLeftPosX;
    int MaxRightPosX;
    //属性
    int Width;
    int Height;
    int Hp;
    int Step;
    

    //移动函数
    void move()
    
signals:

public slots:
};

#endif // ANEMY_H
